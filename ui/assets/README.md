# Branding Assets

Place custom, non-proprietary dashboard branding assets here.

Supported files:

- `logo.png` for the dashboard header and sidebar
- `favicon.png` for browser/page branding when configured
- `company_banner.png` for future branded dashboard surfaces

Recommended logo size: 256 x 256 px PNG with transparent background.
